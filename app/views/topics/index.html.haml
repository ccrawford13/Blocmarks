.content-container
  - if current_user
    .row.topic-form
      .large-12.columns.large-centered
        = render partial: 'form', locals: { user: @user, topics: @topics }
  .row
    .large-10.columns#topics
      = render @topics, locals: { user: @user, topics: @topics }