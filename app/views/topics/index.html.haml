.content-container
  - if policy(@topic).create?
    .row.topic-form
      .large-12.columns.large-centered
        = render partial: 'form', locals: { user: @user, topic: @topic }
  .row
    .large-10.columns#topics
      = render @topics
      = paginate @topics